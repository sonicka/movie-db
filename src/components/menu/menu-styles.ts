import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  toolbar: {
    backgroundColor: "red",
    boxShadow: "2px 2px 8px 5px rgba(255,0,0,0.75)"
  },
  menuItem: {
    width: "50%",
    color: "white",
    fontSize: "13pt",
    fontFamily: "Roboto"
  }
});
